specVersion: "v1"
serviceId: "skeletonauthanddb-d6db9-service"
location: "us-east4"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "skeletonauthanddb-d6db9-database"
        cloudSql:
          instanceId: "skeletonauthanddb-d6db9-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./example"]
